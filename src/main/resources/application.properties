spring.application.name=backend

# These will be overridden by Render environment variables
# But keep them for local development
spring.datasource.url=jdbc:postgresql://localhost:5432/lumen_mall_db
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=10000

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Database initialization
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
logging.level.org.springframework.jdbc.datasource.init=DEBUG

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.max-http-header-size=16KB

# Email settings (update with real credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=angelarknarem@gmail.com
spring.mail.password=dxmz tibk cnuz nlmd 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Payment settings (update with real credentials)
stripe.api.key=
paypal.client.id=
paypal.client.secret=
paypal.mode=sandbox

# Server settings
server.tomcat.max-swallow-size=10MB
server.tomcat.max-http-post-size=10MB
server.port=${PORT:8080}